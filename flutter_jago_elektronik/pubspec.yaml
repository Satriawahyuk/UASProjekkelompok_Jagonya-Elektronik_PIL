name: flutter_jago_elektronik
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  animations: ^2.0.7
  carousel_slider: ^4.2.1
  flutter_screenutil: ^5.6.1
  flutter_rating_bar: ^4.0.1

  # Font, Icon
  google_fonts: ^4.0.3
  cupertino_icons: ^1.0.5

  # Firebase
  firebase_core: ^2.4.1
  firebase_auth: ^4.2.5
  google_sign_in: ^5.4.4
  cloud_firestore: ^4.3.1
  firebase_storage: ^11.0.10

  # Architecture
  get: ^4.6.5

  # Helper
  image_picker: ^0.8.6+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  lint: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/